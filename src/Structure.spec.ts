import { Structure } from './Structure';

describe('Structure', () => {
    beforeEach(async () => {});

    it('Structure should be defined', () => {
        // expect(Structure).toBeDefined();
    });
});
