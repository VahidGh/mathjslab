import { FunctionHandle } from './FunctionHandle';

describe('FunctionHandle', () => {
    it('FunctionHandle and its methods should be defined', () => {
        expect(FunctionHandle).toBeDefined();
    });
});
